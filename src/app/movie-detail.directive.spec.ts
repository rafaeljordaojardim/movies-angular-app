import { MovieDetailDirective } from './movie-detail.directive';

describe('MovieDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieDetailDirective();
    expect(directive).toBeTruthy();
  });
});
